# Ghostty Configuration File
# Automatically created at:
#   /Users/ctejada/Library/Application Support/com.mitchellh.ghostty/config
#
# To see a full list of options and their defaults, run:
#   ghostty +show-config --default --docs
# More details: https://ghostty.org/docs/config

# ────── Theme & Appearance ──────
theme = "Ayu Light"
cursor-style = block

# ────── Text Settings ──────
font-family = FiraCode Nerd Font Mono, MonoLisa
font-size = 13
font-style = Light

# ────── Window Layout ──────
window-padding-x = 15,18
window-padding-y = 8,7
macos-titlebar-proxy-icon = hidden
title = " "
window-colorspace = "display-p3"

# ────── Behavior & Clipboard ──────
copy-on-select = clipboard
confirm-close-surface = false

# ────── Custom Startup Command ──────
# The following command is executed when Ghostty starts.
# Use this to run a custom script or command during startup.
command = ~/Repositories/tilde/scripts/ghostty.sh
