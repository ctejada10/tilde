# Installing useful binaries
sudo apt-get install htop tree stow gpg python3 eza zsh 

# Installing Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh

# Adding user to the `docker` group
sudo usermod -aG docker ctejada

